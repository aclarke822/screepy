// import Utilities from "utils/utilities";

// module.exports = {
//     init: function () {
//         const harvesterBodyParts = [WORK, CARRY, MOVE];
//         const builderBodyParts = [WORK, CARRY, MOVE];
//         const upgraderBodyParts = [WORK, CARRY, MOVE];

//         Memory.creepTypes.push({ name: 'harvester', bodyParts: harvesterBodyParts, role: 'harvester', cost: Utilities.calculateBodyCost(harvesterBodyParts)});
//         Memory.creepTypes.push({ name: 'upgrader', bodyParts: upgraderBodyParts, role: 'upgrader', cost: Utilities.calculateBodyCost(upgraderBodyParts)});
//         Memory.creepTypes.push({ name: 'builder', bodyParts: builderBodyParts, role: 'builder', cost: Utilities.calculateBodyCost(builderBodyParts)});

//         Memory.isInit = true;
//     }
// };